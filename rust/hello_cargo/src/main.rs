fn main() {
    println!("Hello, world!");
    println!("Cargo build to build the project");
    println!("Cargo check to compile without producint the executable (check if it is working)");
    println!("Cargo run to run the executable");

    println!("Cargo build -- release to compile with optimizations (it will take longer)");
}
